fastapi>=0.110.0
uvicorn[standard]>=0.28.0
sqlalchemy>=2.0.28
asyncpg>=0.29.0
alembic>=1.13.1
pydantic>=2.6.4
pydantic-settings>=2.2.1
# Required by pydantic's EmailStr, used across the student profile schemas.
# Was previously only a transitive dependency.
email-validator>=2.1.0
python-jose[cryptography]>=3.3.0
# bcrypt is used directly (app/core/security.py). passlib was dropped: its last
# release (1.7.4, 2020) probes the backend with a >72-byte secret on init, which
# bcrypt >= 5 rejects with ValueError, breaking every hash/verify call.
bcrypt>=4.1.2
python-multipart>=0.0.9
jinja2>=3.1.3
structlog>=24.1.0
httpx>=0.27.0
pytest>=8.1.1
pytest-asyncio>=0.23.6
openpyxl>=3.1.2
# Legacy .xls office files. xlrd 2.x dropped .xlsx support (openpyxl covers
# that) but is still the only reader for the older binary format, which is
# what a college office sending a file from an old Excel install produces.
xlrd>=2.0.1
reportlab>=4.1.0
weasyprint>=61.2
# Vertex AI — only referenced in app/ai/providers/groq.py
groq>=0.9.0
# External service providers
cloudinary>=1.36.0
resend>=2.0.0
